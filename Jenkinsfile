pipeline {
  agent none
  stages {
    stage('Build') {
      steps {
        git(url: 'https://github.com/Patricio-Filice/software-engineering-automatic-deploy.git', branch: 'master')
      }
    }

  }
}